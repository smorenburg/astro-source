---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: argo-workflows
  namespace: argo
spec:
  values:
    server:
      authModes: [ server ]
    workflow:
      serviceAccount:
        annotations:
          azure.workload.identity/client-id: bad30834-40dd-4cac-adfe-29cb18d19d24
